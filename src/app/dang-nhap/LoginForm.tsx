"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function LoginForm() { const router = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); setBusy(true); setMessage(""); const response = await fetch("/api/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email, password }) }); const data = await response.json().catch(() => ({})); if (response.ok) { router.push("/"); router.refresh(); } else setMessage(data.error ?? "Không thể đăng nhập."); setBusy(false); } return <section className="mx-auto max-w-md rounded-xl border-2 border-[#123c36] bg-white p-6 shadow-sm"><p className="text-xs font-semibold tracking-[.16em] text-[#123c36]">TÀI KHOẢN HỆ THỐNG</p><h1 className="mt-1 text-3xl font-semibold">Đăng nhập</h1><p className="mt-2 text-sm text-neutral-900">Bác sĩ, dinh dưỡng viên và người dùng chuyên môn sử dụng tài khoản do Quản trị viên cấp.</p><form onSubmit={submit} className="mt-5 space-y-4"><label className="block font-semibold">Email<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} className="mt-1 w-full rounded border-2 border-[#8fa99e] px-3 py-2" /></label><label className="block font-semibold">Mật khẩu<input required type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="mt-1 w-full rounded border-2 border-[#8fa99e] px-3 py-2" /></label><button disabled={busy} className="w-full rounded bg-[#123c36] px-4 py-2.5 font-semibold text-white">{busy ? "Đang đăng nhập…" : "Đăng nhập"}</button></form>{message && <p className="mt-4 rounded border border-[#8a2323] bg-[#fff5f5] p-3 font-semibold">{message}</p>}<p className="mt-5 text-sm">Chưa có tài khoản? Liên hệ Quản trị viên hoặc nhóm hỗ trợ để được cấp quyền.</p></section>; }
