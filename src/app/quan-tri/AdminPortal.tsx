"use client";

import { FormEvent, useEffect, useState } from "react";
import { defaultSiteSettings } from "@/lib/site-settings";
type User = { id: string; displayName: string; email: string; role: string };
type Contact = { id: string; name: string; email: string | null; phone: string | null; audience: string | null; message: string; status: string; createdAt: string };
type Analytics = { totalVisits: number; newMessages: number; daily: { date: string; visits: number }[]; topPages: { path: string; visits: number }[] };
type Gemini = { enabled: boolean; configured: boolean; model: string };
const input = "mt-1 w-full rounded border-2 border-[#8fa99e] px-3 py-2";

export default function AdminPortal() {
  const [user, setUser] = useState<User | null>(null); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [displayName, setDisplayName] = useState("Quản trị viên");
  const [settings, setSettings] = useState(defaultSiteSettings); const [messages, setMessages] = useState<Contact[]>([]); const [analytics, setAnalytics] = useState<Analytics | null>(null); const [gemini, setGemini] = useState<Gemini>({ enabled: false, configured: false, model: "gemini-2.5-flash" }); const [geminiKey, setGeminiKey] = useState(""); const [notice, setNotice] = useState(""); const [loading, setLoading] = useState(false);
  async function load() { const me = await fetch("/api/auth/me").then((r) => r.json()).catch(() => ({ user: null })); setUser(me.user ?? null); if (me.user?.role === "ADMIN") { const [s, m, a, g] = await Promise.all([fetch("/api/admin/site-settings"), fetch("/api/admin/contact-messages"), fetch("/api/admin/analytics"), fetch("/api/admin/gemini-settings")]); if (s.ok) setSettings({ ...defaultSiteSettings, ...await s.json() }); if (m.ok) setMessages((await m.json()).items); if (a.ok) setAnalytics(await a.json()); if (g.ok) setGemini(await g.json()); } }
  useEffect(() => { void load(); }, []);
  async function authenticate(event: FormEvent) { event.preventDefault(); setLoading(true); const payload = { email, password, displayName }; let response = await fetch("/api/auth/bootstrap", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); if (response.status === 403) response = await fetch("/api/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); const data = await response.json().catch(() => ({})); setNotice(response.ok ? "Đã đăng nhập quản trị." : (data.error ?? "Không thể đăng nhập.")); if (response.ok) await load(); setLoading(false); }
  async function saveSettings(event: FormEvent) { event.preventDefault(); setLoading(true); const response = await fetch("/api/admin/site-settings", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(settings) }); const data = await response.json().catch(() => ({})); if (response.ok) { setSettings({ ...defaultSiteSettings, ...data }); setNotice("Đã lưu thông tin liên hệ."); } else setNotice(data.error ?? "Chưa thể lưu."); setLoading(false); }
  async function saveGemini(event: FormEvent) { event.preventDefault(); setLoading(true); const response = await fetch("/api/admin/gemini-settings", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ enabled: gemini.enabled, model: gemini.model, apiKey: geminiKey }) }); const data = await response.json().catch(() => ({})); if (response.ok) { setGemini(data); setGeminiKey(""); setNotice("Đã lưu cấu hình Gemini. API key được mã hóa và không hiển thị lại."); } else setNotice(data.error ?? "Chưa thể lưu Gemini."); setLoading(false); }
  async function testGemini() { setLoading(true); const response = await fetch("/api/admin/gemini-settings/test", { method: "POST" }); const data = await response.json().catch(() => ({})); setNotice(response.ok ? data.message : (data.error ?? "Không thể kiểm tra Gemini.")); setLoading(false); }
  async function updateMessage(item: Contact, status: string) { const response = await fetch(`/api/admin/contact-messages/${item.id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ status }) }); if (response.ok) { setMessages((rows) => rows.map((row) => row.id === item.id ? { ...row, status } : row)); } }
  async function logout() { await fetch("/api/auth/logout", { method: "POST" }); setUser(null); setNotice("Đã đăng xuất."); }
  if (!user) return <section className="mx-auto max-w-lg rounded-xl border-2 border-[#123c36] bg-white p-6 shadow-sm"><p className="text-xs font-semibold tracking-[.16em] text-[#123c36]">QUẢN TRỊ HỆ THỐNG</p><h1 className="mt-1 text-3xl font-semibold">Đăng nhập quản trị</h1><p className="mt-3">Lần đầu, form này tạo tài khoản quản trị. Các lần sau dùng email và mật khẩu đó để đăng nhập.</p><form onSubmit={authenticate} className="mt-5 space-y-4"><label className="block font-semibold">Họ tên<input required value={displayName} onChange={(e) => setDisplayName(e.target.value)} className={input} /></label><label className="block font-semibold">Email<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} className={input} /></label><label className="block font-semibold">Mật khẩu (ít nhất 10 ký tự)<input required minLength={10} type="password" value={password} onChange={(e) => setPassword(e.target.value)} className={input} /></label><button disabled={loading} className="w-full rounded bg-[#123c36] px-4 py-2.5 font-semibold text-white">{loading ? "Đang xử lý…" : "Đăng nhập / khởi tạo quản trị"}</button></form>{notice && <p className="mt-4 rounded border border-[#8fa99e] bg-[#f1f6f4] p-3 font-semibold">{notice}</p>}</section>;
  if (user.role !== "ADMIN") return <section className="mx-auto max-w-xl rounded-xl border-2 border-[#a77b10] bg-[#fff8df] p-6"><h1 className="text-2xl font-semibold">Tài khoản chưa có quyền quản trị</h1><button onClick={() => void logout()} className="mt-4 rounded border-2 border-[#123c36] px-4 py-2 font-semibold">Đăng xuất</button></section>;
  return <div className="flex flex-col gap-6"><section className="flex flex-wrap items-start justify-between gap-3 border-b-2 border-[#123c36] pb-4"><div><p className="text-xs font-semibold tracking-[.16em] text-[#123c36]">VẬN HÀNH WEBSITE</p><h1 className="mt-1 text-3xl font-semibold">Quản trị Dinh dưỡng 2598</h1><p className="mt-1">Đăng nhập: {user.displayName} · {user.email}</p></div><div className="flex gap-2"><button onClick={() => void load()} className="rounded border-2 border-[#123c36] px-3 py-2 font-semibold">Làm mới</button><button onClick={() => void logout()} className="rounded border-2 border-neutral-500 px-3 py-2 font-semibold">Đăng xuất</button></div></section>{notice && <p className="rounded border-2 border-[#52786d] bg-white p-3 font-semibold">{notice}</p>}<section className="grid gap-px overflow-hidden rounded-lg border-2 border-[#123c36] bg-[#123c36] sm:grid-cols-3"><div className="bg-white p-4"><p>Tổng lượt xem</p><p className="text-3xl font-semibold text-[#123c36]">{analytics?.totalVisits ?? 0}</p></div><div className="bg-white p-4"><p>Liên hệ mới</p><p className="text-3xl font-semibold text-[#123c36]">{analytics?.newMessages ?? 0}</p></div><div className="bg-white p-4"><p>Trang được theo dõi</p><p className="text-3xl font-semibold text-[#123c36]">{analytics?.topPages.length ?? 0}</p></div></section><section className="rounded-xl border-2 border-[#123c36] bg-white p-5"><h2 className="text-xl font-semibold">Thông tin liên hệ công khai</h2><p className="mt-1">Hiển thị ở footer và trang Liên hệ.</p><form onSubmit={saveSettings} className="mt-4 grid gap-4 md:grid-cols-2"><label className="font-semibold">Người phụ trách<input value={settings.contactName} onChange={(e) => setSettings({ ...settings, contactName: e.target.value })} className={input} /></label><label className="font-semibold">Đơn vị / bộ phận<input value={settings.organization} onChange={(e) => setSettings({ ...settings, organization: e.target.value })} className={input} /></label><label className="font-semibold">Điện thoại / Zalo<input value={settings.phone} onChange={(e) => setSettings({ ...settings, phone: e.target.value })} className={input} /></label><label className="font-semibold">Email công khai<input type="email" value={settings.email} onChange={(e) => setSettings({ ...settings, email: e.target.value })} className={input} /></label><label className="font-semibold md:col-span-2">Địa chỉ<input value={settings.address} onChange={(e) => setSettings({ ...settings, address: e.target.value })} className={input} /></label><label className="font-semibold md:col-span-2">Link nhóm Zalo<input value={settings.zaloUrl} onChange={(e) => setSettings({ ...settings, zaloUrl: e.target.value })} className={input} /></label><button disabled={loading} className="justify-self-start rounded bg-[#123c36] px-5 py-2.5 font-semibold text-white">Lưu thông tin liên hệ</button></form></section><section className="rounded-xl border-2 border-[#73540d] bg-[#fffdf6] p-5"><h2 className="text-xl font-semibold">AI Gemini dùng chung</h2><p className="mt-1 text-sm">Người dùng không cần API key. Key chỉ do Admin dán tại đây, được mã hóa trong database và không hiển thị lại.</p><form onSubmit={saveGemini} className="mt-4 grid gap-4 md:grid-cols-2"><label className="flex items-center gap-2 font-semibold md:col-span-2"><input type="checkbox" checked={gemini.enabled} onChange={(e) => setGemini({ ...gemini, enabled: e.target.checked })} />Bật AI Gemini cho người dùng</label><label className="font-semibold">Model<select value={gemini.model} onChange={(e) => setGemini({ ...gemini, model: e.target.value })} className={input}><option value="gemini-2.5-flash">gemini-2.5-flash</option><option value="gemini-2.5-flash-lite">gemini-2.5-flash-lite</option></select></label><label className="font-semibold">Trạng thái<p className="mt-2 font-normal">{gemini.configured ? "Đã lưu key (đã mã hóa)" : "Chưa có API key"}</p></label><label className="font-semibold md:col-span-2">Dán API key Gemini mới<input type="password" autoComplete="new-password" value={geminiKey} onChange={(e) => setGeminiKey(e.target.value)} placeholder={gemini.configured ? "Để trống nếu giữ key hiện tại" : "AIza…"} className={input} /></label><div className="flex flex-wrap gap-3 md:col-span-2"><button disabled={loading} className="rounded bg-[#73540d] px-5 py-2.5 font-semibold text-white">Lưu cấu hình AI</button><button type="button" disabled={loading || !gemini.configured} onClick={() => void testGemini()} className="rounded border-2 border-[#73540d] bg-white px-5 py-2.5 font-semibold text-[#73540d]">Kiểm tra kết nối</button></div></form></section><section className="rounded-xl border-2 border-[#52786d] bg-white p-5"><h2 className="text-xl font-semibold">Tin nhắn liên hệ</h2><div className="mt-4 space-y-3">{messages.map((item) => <article key={item.id} className="border-l-4 border-[#123c36] bg-[#f7faf8] p-4"><div className="flex flex-wrap justify-between gap-2"><p className="font-semibold">{item.name} · {item.audience ?? "Khác"}</p><select value={item.status} onChange={(e) => void updateMessage(item, e.target.value)} className="rounded border border-[#8fa99e] px-2 py-1"><option value="NEW">Mới</option><option value="IN_PROGRESS">Đang xử lý</option><option value="CLOSED">Đã đóng</option></select></div><p className="text-sm">{item.email ?? ""} {item.phone ? `· ${item.phone}` : ""} · {new Date(item.createdAt).toLocaleString("vi-VN")}</p><p className="mt-2 whitespace-pre-wrap">{item.message}</p></article>)}{messages.length === 0 && <p>Chưa có tin nhắn liên hệ.</p>}</div></section></div>;
}
