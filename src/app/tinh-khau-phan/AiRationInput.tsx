"use client";

import { useState } from "react";
type Food = { id: string; name: string; source: string; wastePercent: number | null } & Record<string, unknown>;
export type AiRationItem = { meal: string; dishName: string; foodName: string; edibleGrams: number; note: string; food: Food | null; matchType?: "exact" | "suggested" | "none"; candidates?: Food[] };

const samples = [
  { title: "Món đơn", text: "Sáng: phở bò 350 g." },
  { title: "Món có thành phần rõ", text: "Trưa: cơm chín 200 g. Canh rau ngót thịt 250 g (rau ngót 60 g, thịt lợn nạc 30 g)." },
  { title: "Thực phẩm trực tiếp", text: "Phụ sáng: chuối phần ăn được 100 g, sữa chua 100 g." },
] as const;

export default function AiRationInput({ onConfirm }: { onConfirm: (items: AiRationItem[]) => void }) {
  const [text, setText] = useState(""); const [consent, setConsent] = useState(false); const [userApiKey, setUserApiKey] = useState(""); const [items, setItems] = useState<AiRationItem[]>([]); const [selected, setSelected] = useState<number[]>([]); const [busy, setBusy] = useState(false); const [message, setMessage] = useState("");
  async function parse() { setBusy(true); setMessage(""); setItems([]); setSelected([]); try { const response = await fetch("/api/ai/parse-ration", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ text, externalProcessingConsent: consent, apiKey: userApiKey }) }); const data = await response.json() as { error?: string; items?: AiRationItem[] }; if (!response.ok) { setMessage(data.error ?? "Không thể xử lý yêu cầu AI."); return; } const result = data.items ?? []; setItems(result); setSelected(result.map((item, index) => item.food ? index : -1).filter((index) => index >= 0)); setMessage(`${result.length} dòng được AI bóc tách. Dòng khớp chính xác hoặc gợi ý an toàn được chọn sẵn; hãy rà lại trước khi thêm.`); } catch { setMessage("Không thể gọi AI. Vui lòng thử lại."); } finally { setBusy(false); } }
  function toggle(index: number) { setSelected((current) => current.includes(index) ? current.filter((value) => value !== index) : [...current, index]); }
  function selectCandidate(index: number, foodId: string) { const candidate = items[index]?.candidates?.find((food) => food.id === foodId) ?? null; setItems((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, food: candidate, matchType: candidate ? "suggested" : "none" } : item)); setSelected((current) => candidate ? (current.includes(index) ? current : [...current, index]) : current.filter((value) => value !== index)); }
  const confirmed = items.filter((item, index) => selected.includes(index) && item.food);
  return <section className="rounded-lg border-2 border-[#73540d] bg-[#fffdf6] p-4" data-no-print><div className="border-b-2 border-[#a77b10] pb-3"><p className="text-xs font-semibold tracking-[0.14em] text-[#694d00]">AI HỖ TRỢ NHẬP LIỆU</p><h2 className="mt-1 text-xl font-semibold">Dán mô tả khẩu phần</h2><p className="mt-1 text-sm text-neutral-900">Hệ thống dùng Gemini chung do quản trị viên cấu hình; người dùng không cần API key. AI chỉ bóc tách, không tự sửa dữ liệu gốc hoặc chẩn đoán.</p></div><div className="mt-3 flex flex-wrap gap-2">{samples.map((sample) => <button key={sample.title} type="button" onClick={() => setText(sample.text)} className="rounded border border-[#73540d] bg-white px-3 py-1.5 text-sm font-semibold text-[#694d00]">Dùng mẫu: {sample.title}</button>)}</div><p className="mt-3 rounded-md border-l-4 border-[#a77b10] bg-[#fff9e8] p-3 text-sm text-neutral-950"><b>Để khớp tốt:</b> ghi tên món/thực phẩm và lượng g/ml. Chỉ ghi nguyên liệu trong ngoặc khi bạn biết rõ; nếu không, cứ ghi tên món để hệ thống ưu tiên khớp món ăn có sẵn. Không nhập thông tin định danh bệnh nhân.</p><textarea value={text} onChange={(event) => setText(event.target.value)} placeholder={"Ví dụ: Sáng: phở bò 350 g.\nTrưa: cơm chín 200 g, cá chép kho 80 g."} className="mt-3 min-h-32 w-full rounded-md border-2 border-[#8fa99e] px-3 py-2 text-sm" /><label className="mt-3 flex items-start gap-2 rounded-md border border-amber-700 bg-amber-50 p-3 text-sm text-neutral-950"><input type="checkbox" checked={consent} onChange={(event) => setConsent(event.target.checked)} className="mt-1" /><span>Tôi hiểu mô tả khẩu phần sẽ được gửi đến Gemini để bóc tách và không nhập tên, số điện thoại, địa chỉ, mã bệnh án hay thông tin định danh người bệnh.</span></label><div className="mt-3 flex flex-wrap items-center gap-3"><button type="button" onClick={parse} disabled={busy || !consent || text.trim().length < 8} className="rounded-md bg-[#73540d] px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{busy ? "AI đang bóc tách..." : "Bóc tách bằng AI"}</button><p className="text-xs text-neutral-900">Chỉ người quản trị cấu hình Gemini một lần trên máy chủ; key không hiển thị hay lưu trên trình duyệt.</p></div>{message && <p className="mt-3 text-sm font-semibold text-neutral-950">{message}</p>}{items.length > 0 && <div className="mt-4 overflow-x-auto border-2 border-[#a77b10]"><table className="min-w-[900px] w-full text-sm"><thead><tr><th className="px-3 py-2">Thêm</th><th className="px-3 py-2">Bữa / món</th><th className="px-3 py-2">AI nhận diện</th><th className="px-3 py-2">Khớp CSDL / chọn lại</th><th className="px-3 py-2 text-right">Lượng AI ghi</th><th className="px-3 py-2">Ghi chú</th></tr></thead><tbody>{items.map((item, index) => <tr key={`${item.foodName}-${index}`} className={item.food ? "bg-white" : "bg-[#fff0f0]"}><td className="px-3 py-2 text-center"><input type="checkbox" disabled={!item.food} checked={selected.includes(index)} onChange={() => toggle(index)} /></td><td className="px-3 py-2">{item.meal || "(chưa rõ bữa)"}<br /><span className="text-xs">{item.dishName || "(thực phẩm trực tiếp)"}</span></td><td className="px-3 py-2 font-semibold">{item.foodName}</td><td className="px-3 py-2">{item.food ? <><b>{item.food.name}</b><br /><span className="text-xs font-semibold text-[#123c36]">{item.matchType === "exact" ? "Khớp chính xác" : "Gợi ý cần kiểm tra"} · {item.food.source}</span></> : <span className="font-semibold text-[#8a2323]">Chưa khớp tự động</span>}{(item.candidates?.length ?? 0) > 0 && <select aria-label={`Chọn thực phẩm thay cho ${item.foodName}`} value={item.food?.id ?? ""} onChange={(event) => selectCandidate(index, event.target.value)} className="mt-1 w-full rounded border border-neutral-500 bg-white px-2 py-1 text-xs"><option value="">— Chọn thực phẩm phù hợp —</option>{item.candidates?.map((candidate) => <option key={candidate.id} value={candidate.id}>{candidate.name} · {candidate.source}</option>)}</select>}{!item.food && !(item.candidates?.length) && <p className="mt-1 text-xs text-[#8a2323]">Tìm thủ công hoặc thêm thực phẩm mới.</p>}</td><td className="px-3 py-2 text-right">{item.edibleGrams ? `${item.edibleGrams} g` : "Chưa rõ"}</td><td className="px-3 py-2">{item.note || "—"}</td></tr>)}</tbody></table></div>}{confirmed.length > 0 && <button type="button" onClick={() => { onConfirm(confirmed); setItems([]); setSelected([]); setMessage(`${confirmed.length} dòng đã được thêm vào khẩu phần.`); }} className="mt-4 rounded-md bg-[#123c36] px-4 py-2 font-semibold text-white">Thêm {confirmed.length} dòng đã kiểm tra vào khẩu phần</button>}</section>;
}
